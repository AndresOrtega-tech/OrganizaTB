apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "organizat-backend.fullname" . }}-config
  labels:
    {{- include "organizat-backend.labels" . | nindent 4 }}
data:
  PYTHONUNBUFFERED: "1"
  PORT: "8000"
